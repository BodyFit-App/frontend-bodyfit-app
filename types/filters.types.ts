export type ExerciseFilter = {
  category?: string;
  author?: string;
  title?: string;
};

export type ProgramFilter = {
  category?: string;
  author?: string;
  title?: string;
};

export type ProfileFilter = {
  pseudo?: string;
};
